/*global describe,beforeEach,it*/
/*jshint -W030,-W117*/
'use strict';
var chai = require( 'chai' );
var expect = chai.expect;
var validator = require( './../../lib/config-validator' );

describe( 'Validator', function () {

	//it( 'should be an object', function ( ) {
	//	expect( validator ).to.be.an.object;
	//} );
	//it( 'should expose .validators', function (  ) {
	//	expect( validator ).to.have.property( 'validators');
	//} );
	//
	//it( 'has no validators by default', function (  ) {
	//	expect( validator.validators ).to.have.length(0);
	//} );
	//it( 'should accept new validators', function ( done ) {
	//
	//	validator.use( 'test', function (  ) {
	//		return 'testValue';
	//	});
	//
	//} );

} );